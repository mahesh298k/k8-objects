apiVersion: v1
kind: Pod
metadata:
  name: pod-entrypoint-cmd
  labels:
    app: front-end
    department: human-resource
spec:
  containers:
    - name: pod-entrypoint-cmd
      image: nginx
      command: ["sleep"]
      args: ["10"]